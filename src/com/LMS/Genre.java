package com.LMS;

// Enum representing the genre of a book
/*
Enums provide type safety, meaning that only valid genre values can be assigned
to the genre field of the Book class. This prevents errors that might occur if using
strings or other types to represent genres.
*/
public enum Genre {
    FICTION,
    NON_FICTION
}
